# POS-9 — Validate điều kiện mở/đóng session

Tạo hàm validate_action(table_id/session_id, action) kiểm tra điều kiện hợp lệ trước khi mở hoặc đóng session.

**file_target:** `workspace/backend/app.py`
