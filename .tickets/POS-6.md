# POS-6 — Thêm dịch vụ phụ vào session

Tạo hàm add_service(session_id, service_id, quantity) validate session tồn tại, tạo order item, cập nhật tổng tiền.

**file_target:** `workspace/backend/app.py`
